<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/dialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Custom Dialog Title"
        android:layout_alignParentTop="true"
        android:textSize="20sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/dialogMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/dialogTitle"
        android:layout_marginTop="8dp"
        android:text="..." />

    <Button
        android:id="@+id/dialogButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_below="@+id/dialogMessage"
        android:layout_marginStart="@dimen/fab_margin"
        android:layout_marginTop="16dp"
        android:text="Speak" />

    <ImageButton
        android:id="@+id/imgbtn_stt_share"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/fab_margin"
        android:paddingStart="@dimen/fab_margin"
        android:paddingEnd="@dimen/fab_margin"
        android:src="@android:drawable/ic_menu_share"
        android:background="@drawable/rounded_corner"
        android:layout_toEndOf="@+id/dialogButton"
        android:layout_alignTop="@+id/dialogButton"
        android:layout_alignBottom="@+id/dialogButton"
        android:layout_below="@+id/dialogMessage"/>

</RelativeLayout>